export enum PaymentTypes {
  money = 'money',
  debit = 'debit',
  credit = 'credit',
  pix = 'pix',
}

export enum AppointmentStatus {
  confirm = 'confirm',
  created = 'created',
  cancel = 'cancel',
  expired = 'expired',
}
