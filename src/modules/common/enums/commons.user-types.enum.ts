export enum USER_TYPES {
  CLIENT = 'client',
  PROVIDER = 'provider',
  ADMIN = 'admin',
  INTERNAL = 'internal',
}

export enum USER_TYPES_ID {
  CLIENT = 1,
  PROVIDER = 2,
  ADMIN = 3,
  INTERNAL = 4,
}
